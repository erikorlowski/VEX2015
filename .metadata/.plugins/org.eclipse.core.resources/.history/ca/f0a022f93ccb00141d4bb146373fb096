/*
 * Lift.c
 *
 *  Created on: Mar 12, 2015
 *      Author: Erik
 */

#include "main.h"

Pickup initPickup(PantherMotor motor)
{
	Pickup newPickup = {motor};
	return newPickup;
}

void pickupAtSpeed(Pickup pickup, int speed)
{
	setPantherMotor(pickup.motor, limit(speed, 127, -127));
}
